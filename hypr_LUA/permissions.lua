-------------------
---- PERMISSIONS ---
-------------------

-- See https://wiki.hypr.land/Configuring/Permissions/
-- Please note permission changes here require a Hyprland restart and are not applied on-the-fly
-- for security reasons.

-- hl.config({
--     ecosystem = {
--         enforce_permissions = 1,
--     }
-- })

-- hl.permission({ binary = "/usr/(bin|local/bin)/grim",                               action = "screencopy", allow = true })
-- hl.permission({ binary = "/usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland",    action = "screencopy", allow = true })
-- hl.permission({ binary = "/usr/(bin|local/bin)/hyprpm",                             action = "plugin",     allow = true })
